import styled from 'styled-components';

const Box = styled.div(() => ({
    width: '100%',
    backgroundColor: '#f8f8f8',
    boxShadow: '2px 2px 4px #ccbdbd',
    border: '1px solid #ddd',
    borderRadius: '4px'
}));

export default Box;
